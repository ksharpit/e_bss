{
  "name": "bss-ag",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:provider": "cd provider-app && npx vite --port 5174",
    "dev:user": "cd user-app && npx vite --port 5175",
    "api": "node server/index.js",
    "build": "vite build",
    "build:provider": "cd provider-app && npx vite build",
    "build:user": "cd user-app && npx vite build",
    "build:all": "npm run build && npm run build:provider && npm run build:user",
    "preview": "vite preview"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.12",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "chart.js": "^4.5.1",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  }
}
